import styled from "styled-components";

export const DetailsButtonBox = styled.div`
    display: flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;

    h5 {
        color: var(--gray);
        font-size: 0.75rem;
        font-weight: lighter;
    }
`
